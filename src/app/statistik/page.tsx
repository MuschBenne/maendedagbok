/**
 * Statistik (/statistik): grafer över mående och panikattacker över tid.
 *
 * Platshållare tills steg 6.
 */
import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Statistik" };

export default function StatsPage() {
  return <ComingSoon title="Statistik" step={6} />;
}
